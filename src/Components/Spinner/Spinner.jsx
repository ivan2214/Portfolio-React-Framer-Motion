import React from "react";
import "./Spinner.css";

const Spinner = () => {
  return (
    <section className="spinner-cont">
      <div className="loader"></div>
    </section>
  );
};

export default Spinner;
